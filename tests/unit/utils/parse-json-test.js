import parseJson from 'dummy/utils/parse-json';
import { module, test } from 'qunit';

module('Unit | Utility | parse json');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = parseJson();
  assert.ok(result);
});
